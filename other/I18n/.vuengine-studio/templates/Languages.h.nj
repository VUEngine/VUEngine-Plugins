///////////////////////////////////////////////////////////////////////////////////////////////////////////
//                              THIS FILE WAS AUTO-GENERATED - DO NOT EDIT                               //
///////////////////////////////////////////////////////////////////////////////////////////////////////////

#ifndef LANGUAGES_H_
#define LANGUAGES_H_

//---------------------------------------------------------------------------------------------------------
//                                               DEFINITIONS
//---------------------------------------------------------------------------------------------------------

{% if langs | length -%}
enum LanguageIdentifiers
{
	{% for lang in langs|sort(attribute='order') -%}
	kLanguage{{ lang.id }},
	{%- if not loop.last -%}{{ '\n	' }}{%- endif -%}
	{%- endfor %}
};

{% endif -%}
{% if langs[0].strings | length -%}
enum LanguageStrings
{
	{% for key, string in langs[0].strings -%}
	kString{{ key }},
	{%- if not loop.last -%}{{ '\n	' }}{%- endif -%}
	{%- endfor %}
};

{% endif %}
#endif